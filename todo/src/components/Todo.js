// Instructions: Create a component for each to-do item. Remember, you want to handle state in one place, so no need to call `useState` here. Instead, we want to be able to pass state down to this component.

const Todo = props => {};

export default Todo;
